name: "🔥 Kafka Infrastructure – CI/CD Pipeline"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  SERVICE_NAME: kafka-infrastructure

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-and-build:
    runs-on: [ self-hosted, Linux, X64, srv649924, conexao-de-sorte-kafka-infraestrutura ]
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4.3.0

      - name: Azure Login (OIDC)
        if: >-
          ${{ vars.AZURE_CLIENT_ID != '' && vars.AZURE_TENANT_ID != '' &&
              vars.AZURE_SUBSCRIPTION_ID != '' }}
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Ensure Python deps
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v python3 >/dev/null 2>&1; then
            echo 'python3 não está disponível no runner; instale manualmente.' >&2
            exit 1
          fi
          python3 -c "import importlib, subprocess, sys\ntry:\n    importlib.import_module('yaml')\nexcept ModuleNotFoundError:\n    subprocess.check_call([sys.executable, '-m', 'pip', 'install', '--user', 'PyYAML'])"

      - name: Validate Docker Compose
        run: |
          # Validar sintaxe do docker-compose.yml
          docker compose -f docker-compose.yml config -q
          echo "✅ Docker Compose syntax is valid"

      - name: Security Validation
        run: |
          # Verificar se não há hardcoded passwords
          if grep -r "password.*:" docker-compose.yml | grep -v "\${" | grep -v "#" | grep -v "external:"; then
            echo "❌ Found potential hardcoded passwords"
            exit 1
          else
            echo "✅ No hardcoded passwords found"
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4.5.0
        with:
          name: kafka-configs
          path: |
            docker-compose.yml

  deploy-selfhosted:
    needs: validate-and-build
    runs-on: [ self-hosted, Linux, X64, srv649924, conexao-de-sorte-kafka-infraestrutura ]
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write
    if: github.ref == 'refs/heads/main'
    env:
      DOCKER_NETWORK_NAME: conexao-network-swarm
      STACK_NAME: conexao-kafka
      COMPOSE_FILE: docker-compose.yml
    steps:
      - uses: actions/checkout@v4.3.0
        with:
          clean: true
          fetch-depth: 1

      - name: Azure Login (OIDC)
        if: >-
          ${{ vars.AZURE_CLIENT_ID != '' && vars.AZURE_TENANT_ID != '' &&
              vars.AZURE_SUBSCRIPTION_ID != '' }}
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Download artifacts
        uses: actions/download-artifact@v4.1.8
        with:
          name: kafka-configs
          path: .

      - name: 🔍 Validate Zookeeper Dependency (Soft Check)
        run: |
          echo "🔍 Verificando se Zookeeper está disponível..."

          # Verificar se Zookeeper está rodando na rede (soft dependency)
          if docker service ls | grep -q "conexao-zookeeper_zookeeper"; then
            echo "✅ Zookeeper encontrado no Swarm - prosseguindo"
          else
            echo "⚠️ Zookeeper não encontrado no Swarm"
            echo "💡 Nota: Kafka funciona melhor com Zookeeper em execução"
            echo "💡 Para deploy do Zookeeper: conexao-de-sorte-zookeeper-infraestrutura"
            echo "🔄 Continuando deploy do Kafka (pode falhar se Zookeeper for obrigatório)..."
          fi

      - name: 🚀 Deploy Kafka Stack (Swarm-Only)
        env:
          STACK_NAME: conexao-kafka
          COMPOSE_FILE: docker-compose.yml
        run: |
          echo "🚀 Iniciando deploy do Kafka com Docker Swarm..."

          # Remover stack existente se houver
          if docker stack ls | grep -q "$STACK_NAME"; then
            echo "🔄 Removendo stack existente '$STACK_NAME'..."
            docker stack rm "$STACK_NAME"
            sleep 10  # Kafka cleanup otimizado
          fi

          # Deploy da nova stack
          echo "🏗️ Executando deploy da stack '$STACK_NAME'..."
          docker stack deploy -c "$COMPOSE_FILE" "$STACK_NAME"

          echo "⏰ Aguardando estabilização dos serviços Kafka..."
          sleep 45  # Kafka inicialização otimizada

      - name: Healthcheck Kafka (Robusto)
        env:
          STACK_NAME: conexao-kafka
        run: |
          echo "🔍 Validando saúde do Kafka com health checks robustos..."

          # Aguardar até 4 minutos para Kafka ficar disponível
          timeout=240
          elapsed=0
          health_passed=false

          while [ $elapsed -lt $timeout ] && [ "$health_passed" = false ]; do
            KAFKA_CONTAINER=$(docker ps -q -f name="${STACK_NAME}_kafka" | head -1)

            if [ -n "$KAFKA_CONTAINER" ]; then
              echo "🔍 Testando health check do Kafka... ($elapsed/$timeout segundos)"

              # Método 1: Tentar comando kafka-broker-api-versions
              if docker exec "$KAFKA_CONTAINER" kafka-broker-api-versions --bootstrap-server localhost:9092 >/dev/null 2>&1; then
                echo "✅ Kafka health check passed (API versions)"
                health_passed=true
                break
              fi

              # Método 2: Verificar logs para "started (kafka.server.KafkaServer)"
              if docker logs "$KAFKA_CONTAINER" 2>/dev/null | grep -q "started.*KafkaServer\|Kafka Server.*started"; then
                echo "✅ Kafka health check passed (logs validation)"
                health_passed=true
                break
              fi

              # Método 3: Verificar se porta 9092 está ativa
              if docker exec "$KAFKA_CONTAINER" ss -tuln 2>/dev/null | grep -q ":9092 "; then
                echo "✅ Kafka health check passed (port 9092 active)"
                health_passed=true
                break
              fi

              echo "⏳ Kafka ainda não está pronto... ($elapsed/$timeout segundos)"
            else
              echo "⏳ Container Kafka ainda não encontrado... ($elapsed/$timeout segundos)"
            fi

            sleep 12
            elapsed=$((elapsed + 12))
          done

          if [ "$health_passed" = false ]; then
            echo "⚠️ Kafka health check não passou em $timeout segundos"
            echo "🔍 Verificando logs finais do Kafka..."
            if [ -n "$KAFKA_CONTAINER" ]; then
              echo "--- Últimos 30 logs do Kafka ---"
              docker logs "$KAFKA_CONTAINER" --tail 30 2>/dev/null || true
              echo "--- Fim dos logs ---"
            fi
            echo "💡 Nota: Kafka pode estar funcionando mesmo com health check parcial"
            echo "💡 Verifique os logs acima para diagnóstico"
            # Não fazer exit 1 - permitir deploy continuar
          else
            echo "✅ Kafka health check concluído com sucesso!"
          fi

      - name: Connectivity Validation
        env:
          STACK_NAME: conexao-kafka
        run: |
          echo "🔗 Validando conectividade do Kafka..."

          # Verificar se o serviço está listado no Swarm
          if docker service ls | grep -q "${STACK_NAME}_kafka"; then
            echo "✅ Serviço Kafka encontrado no Swarm"
          else
            echo "❌ Serviço Kafka não encontrado no Swarm"
            exit 1
          fi

          # Verificar se está na rede correta
          if docker network inspect conexao-network-swarm | grep -q kafka; then
            echo "✅ Kafka conectado à rede overlay"
          else
            echo "⚠️ Kafka pode não estar na rede overlay correta"
          fi

          # Verificar conectividade com Zookeeper (soft check)
          KAFKA_CONTAINER=$(docker ps -q -f name="${STACK_NAME}_kafka" | head -1)
          if [ -n "$KAFKA_CONTAINER" ]; then
            if docker exec "$KAFKA_CONTAINER" nc -z conexao-zookeeper 2181 2>/dev/null; then
              echo "✅ Kafka consegue conectar ao Zookeeper"
            else
              echo "⚠️ Kafka não consegue conectar ao Zookeeper (pode ser normal se Zookeeper estiver em outra stack)"
            fi
          fi

          echo "📊 Status final dos serviços:"
          docker service ls --filter name="${STACK_NAME}_*" --format "table {{.Name}}\t{{.Replicas}}\t{{.Image}}"